version: 2

models:


  - name: m_fact_users
    description: fact user view. This grabs all users (who made orders or no orders) and pull together their address, orders (if any), promo and products information.
    columns:
      - name: user_id
        description: unique order identifier
        tests:
          - not_null
          
